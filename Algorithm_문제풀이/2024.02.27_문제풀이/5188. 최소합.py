# 오른쪽, 아래로만 가는 방향 설정
di = [0, 1]
dj = [1, 0]

T = int(input())
for test_case in range(1, T+1):
    N = int(input())
    arr = [list(map(int, input().split())) for _ in range(N)]

    result = 0  # 최소합계 저장할 변수

    


